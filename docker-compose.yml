# ============================================================
# Docker Compose — desenvolvimento local
#
# O banco de dados é o PostgreSQL hospedado no Supabase
# (conectado via DATABASE_URL no .env).
#
# Os serviços `backend` e `ingestion` serão adicionados conforme
# avançarmos no roadmap (passos 4 e 5).
# ============================================================

services: {}
